const baseTailwindConfig = require('../../tailwind.config.js');

/** @type {import('tailwindcss').Config} */
module.exports = {
    ...baseTailwindConfig,
    theme: {
        extend: {},
    },
    plugins: [],
};
